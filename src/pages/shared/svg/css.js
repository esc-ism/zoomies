import {addHorizontalRule, addVerticalRule} from '@/shared/orientation';

import {CLASS_DIAGRAM} from './consts';

addHorizontalRule(`.${CLASS_DIAGRAM}`, {'border-right': '1px solid currentcolor'});
addVerticalRule(`.${CLASS_DIAGRAM}`, {'border-bottom': '1px solid currentcolor'});
