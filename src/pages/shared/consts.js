import {getId} from '@/shared/css';

export const CLASS_DIALOGUE_CONTAINER = getId('dialogue', 'container');
export const CLASS_DIALOGUE_BACKGROUND = getId('dialogue', 'background');
export const CLASS_DIALOGUE = getId('dialogue');
